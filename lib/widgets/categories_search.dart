import 'package:flutter/material.dart';
import 'package:mrkim/constants/styles.dart';

Widget SearchCatergories() => DraggableScrollableSheet(
    initialChildSize: 0.9,
    builder: (_, controller) => Container(
          color: kPrimaryColor,
          child: Column(children: [
            Text("dsgdsgsdag"),
          ]),
        ));
